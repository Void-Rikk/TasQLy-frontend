import type { Tag } from "./model/types.ts";
import { useTags, useCreateTag, useDeleteTag } from "./api/hooks.ts";


export {
    useTags,
    useCreateTag,
    useDeleteTag
}

export type {
    Tag
}